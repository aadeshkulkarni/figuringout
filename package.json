{
  "name": "figuringout",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently --prefix \"[{name}]\" --names \"backend,frontend\" -c \"blue,green\" \"cd backend && npm run dev \" \"cd frontend && npm run dev\" "
  },
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
